namespace XnaGui {
    /// <summary>
    /// Specifies the text alignment on text-based controls.
    /// </summary>
    public enum TextAlign {
        BottomLeft,
        CenterLeft,
        TopLeft,
        TopCenter,
        Center,
        BottomCenter,
        TopRight,
        CenterRight,
        BottomRight
    }
}